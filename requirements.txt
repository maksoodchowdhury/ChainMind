fastapi==0.109.1
uvicorn==0.27.0
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.0
openai>=1.0.0

# RAG engine
llama-index-core==0.10.68.post1
llama-index-llms-openai==0.1.13
llama-index-embeddings-openai==0.1.10
llama-index-vector-stores-qdrant==0.1.4

# Hybrid search — install to enable ENABLE_HYBRID_SEARCH=true
# llama-index-retrievers-bm25>=0.1.0
# rank-bm25>=0.2.2

# Cross-encoder re-ranking — install to enable ENABLE_RERANKING=true
# sentence-transformers>=2.2.0

# RAGAS evaluation — install to enable /api/eval endpoints
# ragas>=0.1.0,<0.2.0
# datasets>=2.0.0

# Redis query caching — install to enable REDIS_URL setting
# redis>=4.0.0

# CSV/Excel ingestion — install to enable .csv and .xlsx uploads
# pandas>=2.0.0
# openpyxl>=3.0.0

# OpenTelemetry tracing — install to enable ENABLE_TRACING=true
# opentelemetry-api>=1.0.0
# opentelemetry-sdk>=1.0.0
# opentelemetry-exporter-otlp-proto-grpc>=1.0.0

# Vector database & API
qdrant-client==1.18.0
python-multipart==0.0.6
aiofiles==23.2.1

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
httpx==0.25.1
requests==2.31.0
